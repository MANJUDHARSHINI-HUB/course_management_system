import { Link } from "react-router-dom";
export default function CourseCard({course}){return <div className="course-card"><div className="course-thumb"><span className="code-tag">{course.code}</span><span className="course-level">{course.level}</span><i className={course.icon}></i></div><div className="course-body"><h3>{course.name}</h3><div className="course-meta"><span>{course.duration}</span><span>{course.instructor}</span></div><p className="desc">{course.desc}</p><Link to={`/enroll-placeholder?course=${encodeURIComponent(course.code)}`} className="btn btn-ghost btn-sm btn-block" onClick={()=>localStorage.setItem("selectedCourseKey",course.code)}>View Course</Link></div></div>}
